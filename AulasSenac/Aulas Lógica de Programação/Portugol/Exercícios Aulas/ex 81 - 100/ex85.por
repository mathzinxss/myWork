programa{
	inclua biblioteca Matematica --> m

    funcao inicio() {
        inteiro n[5][2]
        para (inteiro lin = 0; lin < 5; lin++) {
			para (inteiro col = 0; col < 2; col++) {
				mat[i][j]=u.sorteia(0, 99)
                escreva(mat[i][j])
			}
		}
    }
    /*
        Escrever um algoritmo para ler uma matriz (7,4) contendo valores numéricos (supor que os valores são distintos). Após, encontrar o menor valor contido na matriz e sua posição.
    */
}