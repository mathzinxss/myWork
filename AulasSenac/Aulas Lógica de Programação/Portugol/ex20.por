programa {
    funcao inicio() {
        // TODO Exercício para apresentar sistema de repetição

        // para(variável = vl inicial, ponto de parada, incremento)
        para (inteiro contador = 2; contador <= 100; contador += 2) {
            escreva(contador+" - ")
        }
        escreva("\n\n")   
    }
}
